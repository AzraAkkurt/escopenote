# Fixed Brain API (VPS). Overridden only at build time via VITE_GATEWAY_URL.
VITE_GATEWAY_URL=http://31.57.46.24:3001

# Use in-memory fixture data instead of disk storage (UI dev)
VITE_USE_FIXTURES=false
